{
  "name": "mongodb-service",
  "version": "1.0.0",
  "description": "MongoDB Memory Server for Trust Scoring Platform",
  "scripts": {
    "dev": "bun --hot run index.ts"
  },
  "dependencies": {
    "mongodb-memory-server": "^11.0.1"
  }
}
